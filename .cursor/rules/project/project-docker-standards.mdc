---
description: "Высокоуровневые стандарты и требования к использованию Docker в проекте."
globs: ["Dockerfile*", "docker-compose*.yml", ".dockerignore"]
alwaysApply: false
---

# 📜 Project Docker Standards

Этот документ определяет высокоуровневые стандарты и требования к использованию Docker в проекте. Эти правила являются основой для всех `user-rules` и обязательны для соблюдения при любых изменениях в Docker-инфраструктуре.

---

### 1. Dockerfile & Структура Образов (`P-DOCKER-1`)

-   **P-DOCKER-1.1: Multi-Stage Builds:** Все production-образы **обязаны** использовать multi-stage builds для минимизации размера и поверхности атаки. Отдельные стадии должны быть для сборки зависимостей (`builder`), тестирования (`tester`) и production (`production`).
-   **P-DOCKER-1.2: Non-Root User:** Все production-образы **должны** запускаться от имени пользователя с ограниченными правами (non-root). Имя пользователя и группы должно быть стандартизировано (`appuser`).
-   **P-DOCKER-1.3: Minimal Base Images:** В качестве базовых образов следует использовать минималистичные дистрибутивы (например, `python:3.11-slim`, `node:20-alpine`). Использование "тяжелых" образов (вроде `ubuntu:latest`) запрещено без веского обоснования.
-   **P-DOCKER-1.4: Health Checks:** Каждый production-сервис **обязан** иметь `HEALTHCHECK` инструкцию в Dockerfile для автоматического контроля состояния.

### 2. Docker Compose & Окружение (`P-DOCKER-2`)

-   **P-DOCKER-2.1: Разделение окружений:** Конфигурация для локальной разработки (`docker-compose.yml`) и production (`docker-compose.prod.yml`) **должна** быть разделена. Локальная конфигурация должна поддерживать hot-reload.
-   **P-DOCKER-2.2: Именование сервисов:** Имена сервисов в `docker-compose` должны быть интуитивно понятными и соответствовать их роли (например, `backend`, `frontend`, `postgres`, `redis`).
-   **P-DOCKER-2.3: Управление зависимостями (depends_on):** Зависимости между сервисами должны быть явно указаны с использованием `depends_on` и `condition: service_healthy` для обеспечения корректного порядка запуска.

### 3. Именование и Версионирование (`P-DOCKER-3`)

-   **P-DOCKER-3.1: Стандарт именования образов:** Имена образов должны следовать формату `<project_name>-<service_name>:<version>`. Например: `fullstack-parser-backend:1.2.3`.
-   **P-DOCKER-3.2: Версионирование:** Тег `latest` разрешен только для локальной разработки. В production **должны** использоваться семантические версии (например, `1.2.3`) или хэш коммита.

### 4. Безопасность и Конфигурация (`P-DOCKER-4`)

-   **P-DOCKER-4.1: .dockerignore:** Каждый Docker-контекст **обязан** иметь исчерпывающий файл `.dockerignore` для предотвращения утечки секретов и ускорения сборки.
-   **P-DOCKER-4.2: Управление портами:** Порты, не предназначенные для внешнего доступа, **не должны** публиковаться на хост-машину. Взаимодействие между контейнерами должно происходить внутри Docker-сети.
-   **P-DOCKER-4.3: Монтирование томов (Volumes):** Для разработки разрешено монтировать исходный код. В production следует избегать монтирования кода; вместо этого он должен быть частью образа. Для персистентных данных (БД, кэш) **должны** использоваться именованные тома.

### 5. Тестирование (`P-DOCKER-5`)

-   **P-DOCKER-5.1: Выделенная среда для тестов:** Для запуска тестов **должна** быть создана отдельная конфигурация (например, `docker-compose.test.yml`) или специальная стадия в Dockerfile (`test`), которая не загрязняет production-окружение. 